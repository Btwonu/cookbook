import { ReactComponent } from '../assets/images/avatars/default-av.svg';

const AvatarButton = () => {
  return (
    <div>
      <button>
        <ReactComponent />
      </button>
    </div>
  );
};

export default AvatarButton;
