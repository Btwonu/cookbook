import AuthForm from '../components/Auth/AuthForm';
import Layout from './layouts/Layout';

const Auth = () => {
  return (
    <Layout>
      <AuthForm />
    </Layout>
  );
};

export default Auth;
